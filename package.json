{
  "engines": {
    "node": "16.14.2",
    "npm": "8.5.0"
  },
  "scripts": {
    "start": "npm run start --prefix review-scraper-backend && npm run start --prefix review-scraper-frontend",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix review-scraper-frontend && npm install --prefix review-scraper-backend && npm run build --prefix review-scraper-frontend"
  }
}
