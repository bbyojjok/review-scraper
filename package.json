{
  "engines": {
    "node": "16.14.2",
    "npm": "8.5.0"
  },
  "scripts": {
    "start": "cd review-scraper-backend && npm run start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix review-scraper-backend"
  }
}
